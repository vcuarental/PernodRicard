<apex:component controller="ASI_MFM_CN_POSupplierEmailCtrller" access="global" >
    <apex:attribute name="currentHeaderId" type="String" description=" " assignTo="{!headerId}"/>
    <apex:repeat value="{!SubBrand}" var="subBrand" rendered="{!SubBrand.size > 0}">
        {!subBrand}
        <br/>
    </apex:repeat>
</apex:component>