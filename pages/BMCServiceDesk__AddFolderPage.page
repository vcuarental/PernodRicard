<apex:page showHeader="false" sidebar="false" cache="false" contentType="text/html" controller="BMCServiceDesk.NavigatorController" standardStylesheets="false">
  <c:CommonIncludes />
  <apex:form id="theForm">
   <!--	<apex:outputPanel id="addFolderPanel">
   <script>
			function completeFunction(){
			var ctlinput = document.getElementById('{!$Component.theForm.theInput}');
  			var folderId="{!addedFolderId}"; 
  			//alert('folderobj =='+ folderId);
  			var val = '';
  			if (ctlinput != null) {
  				val = ctlinput.value;
 			}
 			window.parent.setFolderId(folderId);
 			window.parent.setPopUpVar(val);
            window.parent.closePopup();
           } 
	</script>
 </apex:outputPanel>
   	<script>
  		function completeFunction(){
  			var ctlinput = document.getElementById('{!$Component.theForm.theInput}');
  			var val = '';
  			if (ctlinput != null) {
  				val = ctlinput.value;
  			}
  			window.parent.setPopUpVar(val);
            window.parent.closePopup();
  		}
  	</script>
    <apex:actionRegion >
        <table>        
        	<tr  align="center">
	        	 <td class="clsInputLabelTD" align="left" ><apex:outputLabel value="{!$Label.folderNameAddFolderPage}"/></td>
	        	 <td class="clsInputTextBoxTD" align="left" ><apex:inputText id="theInput" value="{!strTextValue}" /></td>
        			        	 
        	</tr>
        	<tr>
        		<td></td>
        		<td></td> 		
        	</tr>
        	<tr>
        		<td align="right"> <apex:commandButton id="saveButton" value="{!$Label.saveAddFolderPage}" action="{!addFolder}"   rerender="addFolderPanel" oncomplete="completeFunction();" /></td>
        		<td><apex:commandButton id="cancleButton" value="{!$Label.cancel}" onclick="window.parent.closePopup();"/></td>
        	</tr>        	
        </table> 
     </apex:actionRegion> -->
   </apex:form>
    <apex:outputpanel id="xyz"/>
</apex:page>