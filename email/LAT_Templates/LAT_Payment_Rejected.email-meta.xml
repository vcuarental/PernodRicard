<?xml version="1.0" encoding="UTF-8"?>
<EmailTemplate xmlns="http://soap.sforce.com/2006/04/metadata">
    <available>true</available>
    <description>Use to notify when a payment is rejected in the approval process</description>
    <encodingKey>UTF-8</encodingKey>
    <name>LAT_Payment_Rejected</name>
    <style>none</style>
    <subject>Pagamento reprovado</subject>
    <textOnly>{!LAT_Payment__c.CreatedBy}

O pagamento {!LAT_Payment__c.Name} do contrato {!LAT_Payment__c.LAT_Contract__c} foi reprovado

Clique aqui para acessar o pagamento no SF Web: {!LAT_Payment__c.Link}

Att.

CRM Team</textOnly>
    <type>custom</type>
    <uiType>Aloha</uiType>
</EmailTemplate>
